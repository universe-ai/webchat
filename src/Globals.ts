/**
 * Export of globals.
 *
 */

import {
    BrowserUtil,
    BrowserFileStreamReader,
    Service,
    ParseUtil,
    Universe,
} from "universeai";

import {
    PresenceController,
} from "./controllers/PresenceController";

export default {
    BrowserUtil,
    BrowserFileStreamReader,
    Service,
    Universe,
    ParseUtil,
    PresenceController,
}
