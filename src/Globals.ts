/**
 * Export of globals.
 *
 */

import {
    BrowserUtil,
    BrowserFileStreamReader,
    Service,
    ParseUtil,
    Universe,
} from "universeai";

import {
    PresenceController,
    ChannelsController,
    ChannelController,
    NotificationsController,
} from "./controllers";

export default {
    BrowserUtil,
    BrowserFileStreamReader,
    Service,
    Universe,
    ParseUtil,
    PresenceController,
    ChannelsController,
    ChannelController,
    NotificationsController,
}
